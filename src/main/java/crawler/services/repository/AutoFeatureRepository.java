package crawler.services.repository;

import crawler.core.entities.AutoFeature;
import org.springframework.data.repository.CrudRepository;

public interface AutoFeatureRepository extends CrudRepository<AutoFeature, String> {
}
